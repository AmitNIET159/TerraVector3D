# BhuDrishti 3D — Module Integration Guide
The backend integrates with 4 approved standalone modules without modifying them.
`module_adapter.py` adds project root to `sys.path`.
Services handle adaptation.
